# Open Questions

- Does the Linux/Hermes environment have `YOUTUBE_API_KEY` available?
- After the first real run, what confidence threshold gives the right balance between coverage and correctness?
- Should GitHub Pages deploy from `main` or a later workflow branch once the repo is made public?
- Should future data refresh be manual, scheduled through GitHub Actions, or triggered from Hermes?
- Are there additional channel-specific negative/positive phrases to add after observing generated candidates?
